@using Framework.Application 
@model IEnumerable<StoreManagement.Application.Contract.PackageAgg.PackageVM> 
@{
    ViewData["Title"] = "خرید اشتراک";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="best-seller-title my-5">
    <h4>پکیج ها فروشگاه</h4>
</div>


<!-- products -->
<div class="container border p-3">
    <div class="packages">
        <div class="row justify-content-around">
            @if (Model.Any())
            {
                @foreach (var package in Model)
                {
                    <div class="col-sm-12 col-md-3 package rounded">
                        <div class="package-card text-center">
                            <div class="image-package">
                                <img src="~/Pictures/@package.ImageName" alt="@package.Title">
                            </div>
                            <div class="detail-package my-3">
                                <div class="title-package">
                                    <p class="h4">@package.Title</p>
                                    <p class="h6">ماهیانه @package.Cost.ToMoney() تومان</p>
                                </div>
                                <hr>
                                <div class="detail-package">
                                    <p>
                                        @package.Description
                                    </p>
                                </div>
                                <div class="btn-package">
                                    <a href="" class="btn btn-outline-danger">پرداخت</a>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            }
            else
            {
                <p class="alert alert-warning">در حال حاضر هیچ پکیجی برای خرید وجود ندارد</p>
            }

        </div>
    </div>

</div>


