@model TicketManagement.Application.Contract.TicketAgg.AddMessageTicketVM

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h4 class="modal-title">ارسال پاسخ به تیکت </h4>
</div>

<form asp-controller="Ticket" asp-action="SendMessage"
      method="post"
      data-ajax="true"
      data-callback=""
      data-action="Refresh"
      enctype="multipart/form-data">
    <div class="modal-body">
        <div class="row">
            <div class="col-md-12">
                <div class="form-group">
                    <label asp-for="Text" class="control-label">پاسخ</label>
                    <textarea class="form-control" asp-for="Text"></textarea>
                    <span asp-validation-for="Text" class="error"></span>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <input type="hidden" asp-for="TicketId" />
        <button type="button" class="btn btn-default waves-effect" data-dismiss="modal">بستن</button>
        <button type="submit" class="btn btn-info waves-effect waves-light">ارسال پیام</button>
    </div>
</form>